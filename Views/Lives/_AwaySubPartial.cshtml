@model MyField.ViewModels.AwaySubViewModel


<div>
    <form id="awaySubForm" method="post">
        @Html.AntiForgeryToken()

        <div class="form-group row">
            <label for="outAwayPlayer" class="col-sm-3 col-form-label"><i class="fa fa-arrow-down" style="font-size:14px; color:red"></i> Out:</label>
            <div class="col-sm-9">
                <select id="outAwayPlayer" name="outAwayPlayer" class="form-control">
                    <option value="">Select Player</option>
                    @if (Model?.StartingXi != null && Model.StartingXi.Any())
                    {
                        foreach (var player in Model.StartingXi)
                        {
                            <option value="@player.PlayerId">
                                @player.FullName (@player.JerseyNumber)
                            </option>
                        }
                    }
                    else
                    {
                        <option value="">No players available</option>
                    }
                </select>
            </div>
        </div>

        <div class="form-group row mt-3">
            <label for="inAwayPlayer" class="col-sm-3 col-form-label"><i class="fa fa-arrow-up" style="font-size:14px; color: green"></i>  In:</label>
            <div class="col-sm-9">
                <select id="inAwayPlayer" name="inAwayPlayer" class="form-control">
                    <option value="">Select Player</option>
                    @if (Model?.Substitutes != null && Model.Substitutes.Any())
                    {
                        foreach (var player in Model.Substitutes)
                        {
                            <option value="@player.PlayerId">
                                @player.FullName (@player.JerseyNumber)
                            </option>
                        }
                    }
                    else
                    {
                        <option value="">No players available</option>
                    }
                </select>
            </div>
        </div>
    </form>

</div>
